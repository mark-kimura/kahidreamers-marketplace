---
description: Browse public example Datatrove threads (the Gallery)
argument-hint: [optional thread-id to open]
allowed-tools: mcp__plugin_datatrove_datatrove__browse_gallery, mcp__plugin_datatrove_datatrove__get_public_thread
---

Show the user public example Datatrove threads. Arguments: **$ARGUMENTS**

Call `browse_gallery` and list recent public threads (title, id, owner). If the user picks one, open it with `get_public_thread` and summarize its discovery + analysis. No sign-in required.
